# 项目协作规范

本文档定义本项目的代码、文档、任务和交付约束。除非有更高优先级的明确要求，所有新增实现与协作流程都应遵循本文件。

## 1. 文档目录约束

- 所有正式文档统一放在 `docs/` 或其子目录下。
- 例外仅限项目根目录的 `README.md` 和 `AGENTS.md`。
- `docs/` 下存放项目设计文档、说明文档、接口文档、架构文档、部署文档、运维文档等长期有效内容。
- `docs/` 可以按需要建立层级结构，例如：
  - `docs/design/`
  - `docs/requirements/`
  - `docs/guides/`
  - `docs/interfaces/`
  - `docs/changelog.md`

## 2. 临时文档约束

- 过程文档、交接文档、测试报告、调试记录、阶段性分析、临时排查说明等临时性材料统一放在 `temp_docs/` 或其子目录下。
- `temp_docs/` 不应被 Git 追踪，必须加入忽略规则。
- 临时文档应及时清理，避免长期堆积。
- 若某份临时文档后续演变为长期有效文档，应迁移到 `docs/` 下的合适位置。

## 3. 任务文档约束

- 任务计划、任务进度、执行清单、里程碑跟踪等文档统一放在 `tasks/` 或其子目录下。
- `tasks/` 下的文档应被 Git 追踪。
- 已完成任务的任务文档应及时清理；若其中包含需要长期保留的信息，应提炼后转入 `docs/`。
- `tasks/` 可以按需要建立层级结构，例如：
  - `tasks/in_progress/`
  - `tasks/completed/`
  - `tasks/archive/`

## 4. 需求与变更记录

- 所有新功能实现前，必须先具备对应的需求文档。
- 新功能需求文档应放在 `docs/requirements/` 或更合适的正式文档目录下。
- 所有功能实现情况都必须记录到变更日志中。
- 项目统一使用 `docs/changelog.md` 记录功能实现、重要修复、兼容性变更和行为调整。

## 5. 语言规范

- 文档内容统一使用中文编写。
- 代码注释必须使用英文。
- 日志输出必须使用英文。
- 面向命令行的帮助信息、控制台输出、错误信息应优先使用英文，并遵循 POSIX 风格与约定。

## 6. 路径规范

- 代码和文档中都不允许出现绝对路径。
- 文档中的路径一律相对于项目根目录书写。
- 可执行程序使用的相对路径应相对于可执行文件自身。
- 动态库使用的相对路径应相对于动态库自身。
- 对于平台强制规定资源文件放置位置的场景，可以放宽“相对某个模块”的限制，但仍然禁止出现绝对路径。

## 7. 实现与验证要求

- 所有已完成功能都必须进行自测，并对结果进行检验。
- 若受客观条件限制无法完成自测或检验，必须主动说明阻塞原因。
- 若缺少测试或检验所需环境、依赖、设备、服务、账号或工具，必须明确列出缺失项。
- 提交实现结果时，应同时说明：
  - 已执行的自测内容
  - 检验结果
  - 未完成验证的原因
  - 继续验证所需补充条件

## 8. 建议目录结构

建议采用如下结构，并按实际项目复杂度逐步细化：

```text
.
├── AGENTS.md
├── README.md
├── docs/
│   ├── changelog.md
│   ├── design/
│   ├── guides/
│   ├── interfaces/
│   └── requirements/
├── tasks/
│   ├── completed/
│   └── in_progress/
└── temp_docs/
```

## 9. 执行约定

- 新增正式文档时，优先判断应归档到 `docs/` 的哪个子目录，而不是直接堆在根目录。
- 新增任务文档时，优先放在 `tasks/` 的合适层级。
- 新增功能开发时，若缺少需求文档，应先补需求文档再实施。
- 完成功能后，应同步更新 `docs/changelog.md`。
- 清理文档时，应区分正式沉淀内容与临时过程材料，避免误删长期有效文档。
- 每完成一条独立 feature，都应立即产生一次 Git 提交，并尽快推送到 GitHub 远端。
- 提交粒度应尽量对应单一功能闭环，避免把多个无关 feature 混在同一次提交中。
- 当用户要求将回复写入 `temp_docs/reply/` 时，后续回复默认不直接输出到屏幕，而应按数字序号写入该目录下的 Markdown 文件，例如 `0.md`、`1.md`、`2.md`。
- 所有功能完成后都必须进行自测，并记录实际执行的自测内容与结果。
- 若功能涉及 Android 客户端，默认必须使用本机已安装的 Android 模拟器完成自测；若模拟器不可用，必须明确说明阻塞原因与缺失条件。

## 10. Hub 服务约定

- Hub 是项目核心服务，负责 agent 注册、在线状态、会话时间线和命令转发。
- 任务完成后，必须确保 Hub 服务处于启动状态，以便用户进行后续测试。
- Hub 默认监听 `http://0.0.0.0:8787`，可通过 `npm run dev:hub` 或 `node_modules/.bin/tsx apps/hub/src/server.ts` 启动。
- 验证 Hub 是否正常：`curl http://127.0.0.1:8787/healthz` 应返回 `{"status":"ok",...}`。
- 若任务执行过程中需要重启 Hub，应在任务完成后再次启动并确认运行正常。
